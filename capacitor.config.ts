import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deepblue.moneytracker',
  appName: 'Money Tracker',
  webDir: 'dist'
};

export default config;
