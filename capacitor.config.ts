import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deepblue.pocketexpense',
  appName: 'PocketExpense',
  webDir: 'dist'
};

export default config;
